
  # User Dashboard Design (Community)

  This is a code bundle for User Dashboard Design (Community). The original project is available at https://www.figma.com/design/7ozL1wMdPPdEEpwsjiKuVM/User-Dashboard-Design--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  